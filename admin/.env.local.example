# Same Supabase project as backend — use the service key (server-side only, never in browser)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# Simple admin password — protect this URL during the pilot
ADMIN_PASSWORD=choose-a-strong-password
